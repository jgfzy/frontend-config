{
  "name": "@jgfzy/frontend-config",
  "version": "1.1.0",
  "description": "This is a front-end normalization configuration package that helps refine configurations quickly, but can't be used in extensions",
  "main": "index.js",
  "scripts": {
    "dev": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint:eslint": "eslint --fix",
    "lint:prettier": "prettier --write",
    "lint:stylelint": "stylelint --fix"
  },
  "keywords": [
    "eslint-config",
    "prettier-config",
    "stylelint-config",
    "sass-config",
    "vue-config",
    "typescript-config",
    "vscode-config"
  ],
  "author": "jgfzy (https://github.com/jgfzy)",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "packageManager": "pnpm@10.11.0",
  "devDependencies": {}
}
